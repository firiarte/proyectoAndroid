<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="boa.mysportsboa" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logomysports"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--<activity-->
            <!--android:name=".Adapter.eventoActivity"-->
            <!--android:label="@string/title_activity_evento"-->
            <!--android:parentActivityName=".MainActivity" >-->
            <!--<meta-data-->
                <!--android:name="android.support.PARENT_ACTIVITY"-->
                <!--android:value="boa.mysportsboa.MainActivity" />-->
        <!--</activity>-->
        <activity
            android:name=".eventoActivity"
            android:label="@string/title_activity_evento"
            android:parentActivityName=".MainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="boa.mysportsboa.MainActivity" />
        </activity>
        <activity
            android:name=".equipoActivity"
            android:label="@string/title_activity_equipo"
            android:parentActivityName=".MainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="boa.mysportsboa.MainActivity" />
        </activity>
    </application>

</manifest>
